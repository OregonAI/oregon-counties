# src/ingest_counties.py extracts county PDFs via src/extract.py. Same extractor as
# oregon-audits and federal-reference, so the page-furniture and heading-guard behaviour is
# shared rather than a third implementation that drifts from the other two.
pypdf
PyYAML

# src/ingest_counties.py's js-render discovery mode. Used ONLY for hosts that serve an
# honestly-identified agent and build their document lists client-side — never to get
# past a host that refused one. See discover_js().
playwright

# src/fetch.py speaks HTTP/2. This is not a preference: codepublishing.com and
# lake.county.codes challenge HTTP/1.1 requests and pass HTTP/2 ones with the SAME
# User-Agent, so urllib (HTTP/1.1 only) was scored as a bot and refused. h2 is required
# for httpx's http2=True and is not pulled in by httpx alone.
httpx
h2

# THE SERVER ITSELF. Absent until the first attempt to build a deployable image, which
# failed at the Dockerfile's ensure_index step with `ModuleNotFoundError: No module named
# 'corpus_toolkit'`.
#
# Nothing surfaced it earlier because CI never reads this line: every validation job is a
# corpus-toolkit REUSABLE WORKFLOW that installs the toolkit itself from its own
# `toolkit-ref` input. So the repo could be fully green, on the current pin, in a corpus
# whose every gate exercises the toolkit — while the file that a container installs from
# had never heard of it.
#
# A NEW PIN SITE. A toolkit bump must now move this line too, alongside the workflow refs;
# corpus-toolkit#9 is about exactly the version skew that follows from missing one.
corpus-toolkit[mcp] @ git+https://github.com/OregonAI/corpus-toolkit@v1.17.0
